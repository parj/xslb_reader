[project]
name = "xlsb-reader"
version = "0.1.0"
description = "Reads XSLB files and prints formulas"
readme = "README.md"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest", "ruff", "ty"]
